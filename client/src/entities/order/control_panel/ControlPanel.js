import React, {useState} from 'react';

export default function ControlPanel({}){

    // как лучше назвать BottomControlPanel ?? или как
    // где должен находиться панель контроля (в дизайне он появляется только в контексте заказа)
    // значит его не нужно писать в widgets, т.к это не независимый блок приложения

    return (
        <>
        
        </>
    );
}

